{
	"devDependencies": {
		"@types/concurrently": "^6.0.0",
		"typescript": "^4.1.5"
	},
	"scripts": {
		"start:db": "json-server -w db.json",
		"start:parcel": "parcel index.html",
		"dev": "concurrently \"json-server -w db.json\" \"parcel index.html\""
	},
	"dependencies": {
		"axios": "^0.21.1",
		"concurrently": "^6.0.0"
	}
}
